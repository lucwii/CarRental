@model CarRental.Models.Domain.Location
@{
    ViewData["Title"] = "Add Location";
}

@await Html.PartialAsync("_AdminLayout")

<section class="auth-page" style="min-height: 100vh; padding-top: 7rem;">
    <div class="auth-bg-grid"></div>
    <div class="auth-glow auth-glow-1"></div>
    <div class="auth-glow auth-glow-2"></div>

    <div class="auth-container">
        <div class="auth-card">
            <div class="auth-card-header">
                <div class="auth-logo">
                    <div class="nav-logo-icon">
                        <svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="white" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round">
                            <path d="M21 10c0 7-9 13-9 13s-9-6-9-13a9 9 0 0118 0z"/><circle cx="12" cy="10" r="3"/>
                        </svg>
                    </div>
                </div>
                <h1 class="auth-title">Add Location</h1>
                <p class="auth-subtitle">Add a new pickup/drop-off point</p>
            </div>

            <form action="/Admin/Locations/Create" method="post" class="auth-form">
                @Html.AntiForgeryToken()
                <div asp-validation-summary="ModelOnly" class="auth-error-summary"></div>

                <div class="auth-field">
                    <label asp-for="City" class="auth-label">City</label>
                    <div class="auth-input-wrapper">
                        <svg class="auth-input-icon" width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                            <path d="M21 10c0 7-9 13-9 13s-9-6-9-13a9 9 0 0118 0z"/><circle cx="12" cy="10" r="3"/>
                        </svg>
                        <input asp-for="City" class="auth-input" placeholder="e.g. Beograd, Novi Sad" />
                    </div>
                    <span asp-validation-for="City" class="auth-field-error"></span>
                </div>

                <div class="auth-field">
                    <label asp-for="Address" class="auth-label">Address</label>
                    <div class="auth-input-wrapper">
                        <svg class="auth-input-icon" width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                            <path d="M3 9l9-7 9 7v11a2 2 0 01-2 2H5a2 2 0 01-2-2z"/><polyline points="9 22 9 12 15 12 15 22"/>
                        </svg>
                        <input asp-for="Address" class="auth-input" placeholder="e.g. Knez Mihailova 22" />
                    </div>
                    <span asp-validation-for="Address" class="auth-field-error"></span>
                </div>

                <button type="submit" class="auth-btn">
                    Add Location
                    <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round">
                        <path d="M5 12h14M12 5l7 7-7 7"/>
                    </svg>
                </button>
            </form>

            <div class="auth-divider"><span></span></div>

            <div class="auth-footer">
                <p><a href="/Admin/Locations" class="auth-link">Back to Manage Locations</a></p>
            </div>
        </div>
    </div>
</section>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
