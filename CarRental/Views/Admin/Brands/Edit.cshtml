@model CarRental.Models.Domain.Brand
@{
    ViewData["Title"] = $"Edit {Model.Name}";
}

@await Html.PartialAsync("_AdminLayout")

<section class="auth-page" style="min-height: 100vh; padding-top: 7rem;">
    <div class="auth-bg-grid"></div>
    <div class="auth-glow auth-glow-1"></div>
    <div class="auth-glow auth-glow-2"></div>

    <div class="auth-container">
        <div class="auth-card">
            <div class="auth-card-header">
                <div class="auth-logo">
                    <div class="nav-logo-icon">
                        <svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="white" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round">
                            <path d="M11 4H4a2 2 0 00-2 2v14a2 2 0 002 2h14a2 2 0 002-2v-7"/><path d="M18.5 2.5a2.121 2.121 0 013 3L12 15l-4 1 1-4 9.5-9.5z"/>
                        </svg>
                    </div>
                </div>
                <h1 class="auth-title">Edit Brand</h1>
                <p class="auth-subtitle">Update details for @Model.Name</p>
            </div>

            <form action="/Admin/Brands/Edit/@Model.Id" method="post" class="auth-form">
                @Html.AntiForgeryToken()
                <div asp-validation-summary="ModelOnly" class="auth-error-summary"></div>
                <input type="hidden" asp-for="Id" />

                <div class="auth-field">
                    <label asp-for="Name" class="auth-label">Brand Name</label>
                    <div class="auth-input-wrapper">
                        <svg class="auth-input-icon" width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                            <circle cx="12" cy="12" r="10"/><path d="M8 12h8M12 8v8"/>
                        </svg>
                        <input asp-for="Name" class="auth-input" />
                    </div>
                    <span asp-validation-for="Name" class="auth-field-error"></span>
                </div>

                <div class="auth-field">
                    <label asp-for="Country" class="auth-label">Country</label>
                    <div class="auth-input-wrapper">
                        <svg class="auth-input-icon" width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                            <circle cx="12" cy="12" r="10"/><line x1="2" y1="12" x2="22" y2="12"/><path d="M12 2a15.3 15.3 0 014 10 15.3 15.3 0 01-4 10 15.3 15.3 0 01-4-10 15.3 15.3 0 014-10z"/>
                        </svg>
                        <input asp-for="Country" class="auth-input" />
                    </div>
                    <span asp-validation-for="Country" class="auth-field-error"></span>
                </div>

                <button type="submit" class="auth-btn">
                    Save Changes
                    <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round">
                        <path d="M20 6L9 17l-5-5"/>
                    </svg>
                </button>
            </form>

            <div class="auth-divider"><span></span></div>

            <div class="auth-footer">
                <p><a href="/Admin/Brands" class="auth-link">Back to Manage Brands</a></p>
            </div>
        </div>
    </div>
</section>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
